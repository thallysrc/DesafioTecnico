---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/tsconfig.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/index.html
  - frontend/.gitignore
  - frontend/.eslintrc.cjs
  - frontend/.prettierrc
  - frontend/env.d.ts
  - frontend/public/favicon.svg
  - frontend/src/main.ts
  - frontend/src/App.vue
  - frontend/src/style.css
  - frontend/src/router/index.ts
  - frontend/src/shared/api/client.ts
  - frontend/src/shared/api/health.ts
  - frontend/src/shared/components/AppShell.vue
  - frontend/src/shared/components/HealthPill.vue
  - frontend/src/features/products/pages/ProductsPage.vue
  - frontend/src/features/stock/pages/StockMovementsPage.vue
  - frontend/src/features/products/.gitkeep
  - frontend/src/features/stock/.gitkeep
autonomous: true
requirements:
  - FRONT-01
  - FRONT-02
  - FRONT-03
  - FRONT-04
  - FRONT-09
  - FRONT-10
  - FRONT-11
  - INFRA-08
must_haves:
  truths:
    - "`npm run dev` (or via docker-compose) serves the SPA at http://localhost:5173"
    - "`npm run build` produces a clean production bundle with zero TypeScript errors under strict mode"
    - "Visiting `/` redirects to `/products`; `/products` and `/stock-movements` both resolve to their placeholder pages (FRONT-10)"
    - "Sidebar renders the wordmark `Stock<span class=\"text-brand-500\">Easy</span>` at the top (FRONT-11)"
    - "Sidebar nav uses lucide-vue-next icons (Package, ArrowLeftRight) and active item gets `bg-brand-50 text-brand-700`"
    - "Health pill at bottom of sidebar fetches `/api/health` on mount via Axios (baseURL `/api`) and renders one of three states: checking / connected / offline"
    - "Vite proxies `/api/**` to the literal `http://backend:8080` (D-06, D-07 — no env-var indirection) — Axios calls written as `/health` resolve via the proxy"
    - "Tailwind `brand-*` palette (50..900) + semantic tokens are wired in `tailwind.config.js` (FRONT-02)"
    - "Inter font loaded via Google Fonts in `index.html` with weights 400/500/600/700 (FRONT-03)"
    - "TypeScript strict mode enabled in `tsconfig.json` (FRONT-01)"
    - "Feature-based folder layout: `src/features/products/`, `src/features/stock/`, `src/shared/` (FRONT-04)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Dependencies + scripts (dev, build, preview, lint, format)"
      contains: "\"vue\":"
    - path: "frontend/vite.config.ts"
      provides: "Vite config with /api proxy → literal http://backend:8080 (D-06, D-07 — no env-var indirection)"
      contains: "/api"
    - path: "frontend/tailwind.config.js"
      provides: "Brand palette + semantic tokens + Inter as font-sans"
      contains: "brand"
    - path: "frontend/index.html"
      provides: "Inter font link + #app root + title StockEasy"
      contains: "fonts.googleapis.com/css2?family=Inter"
    - path: "frontend/src/App.vue"
      provides: "Root layout = AppShell + router-view, fetches /api/health on mount"
      contains: "router-view"
    - path: "frontend/src/shared/components/AppShell.vue"
      provides: "Sidebar with wordmark, nav (Produtos, Movimentação de Estoque), and HealthPill"
      contains: "StockEasy"
    - path: "frontend/src/shared/components/HealthPill.vue"
      provides: "Three-state health indicator (checking/connected/offline)"
      contains: "Verificando API"
    - path: "frontend/src/router/index.ts"
      provides: "Routes /products, /stock-movements + redirect / → /products"
      contains: "redirect"
    - path: "frontend/src/shared/api/client.ts"
      provides: "Single Axios instance with baseURL '/api'"
      contains: "baseURL"
  key_links:
    - from: "frontend/src/App.vue"
      to: "frontend/src/shared/components/AppShell.vue"
      via: "<AppShell> wraps <router-view/>"
      pattern: "AppShell"
    - from: "frontend/src/shared/components/HealthPill.vue"
      to: "GET /api/health via Axios client"
      via: "onMounted → apiClient.get('/health')"
      pattern: "/health"
    - from: "frontend/vite.config.ts"
      to: "http://backend:8080"
      via: "server.proxy '/api' (literal target, no env var)"
      pattern: "http://backend:8080"
    - from: "frontend/src/shared/api/client.ts"
      to: "Vite proxy"
      via: "baseURL: '/api'"
      pattern: "baseURL: '/api'"
---

<objective>
Scaffold the Vue 3 + Vite + TypeScript-strict frontend SPA under `frontend/`. Wire Tailwind with the full brand palette (FRONT-02), load Inter font (FRONT-03), establish the feature-based folder layout (FRONT-04), set up Vue Router 4 with the two placeholder routes + root redirect (FRONT-09, FRONT-10), build the sidebar `AppShell` with the colorized `Stock<span class="text-brand-500">Easy</span>` wordmark (FRONT-11), wire a single Axios instance with `baseURL: '/api'` (D-08), and configure Vite's dev-server proxy `/api → http://backend:8080` (D-06, D-07 — literal target, no env-var indirection). Render a health pill at the bottom of the sidebar that calls `GET /api/health` on mount and shows checking / connected / offline.

Purpose: Deliver FRONT-01..04, FRONT-09..11, and INFRA-08 (Vite serves on :5173 with hot reload). After this plan, opening `http://localhost:5173` shows the StockEasy SPA shell.
Output: Working Vite dev server, two placeholder routes rendering, sidebar with wordmark + nav + health pill calling the backend via the proxy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/REQUIREMENTS.md
@frontend/CLAUDE.md

<interfaces>
<!-- Locked from frontend/CLAUDE.md (NON-NEGOTIABLE) -->
<!-- Stack: Vue 3 + <script setup> + TS strict + Vite 5 + Tailwind 3 + Vue Router 4 + Axios + lucide-vue-next -->
<!-- NO Pinia, NO UI library, NO dayjs/lodash/jQuery -->
<!-- Feature-based: src/features/products/, src/features/stock/, src/shared/ -->
<!-- Wordmark: Stock<span class="text-brand-500">Easy</span> -->
<!-- Sidebar: light (bg-white), item ativo bg-brand-50 text-brand-700 -->

<!-- Locked from CONTEXT.md D-06..D-08: -->
<!-- D-06: Vite proxy /api/** → http://backend:8080 -->
<!-- D-07: Target is the literal compose service name "backend" (NOT host.docker.internal, NOT env-driven). No process.env.BACKEND_HOST, no ternary, no fallback. -->
<!-- D-08: Axios baseURL = '/api'. Calls like apiClient.get('/health') resolve to /api/health via proxy. -->
<!-- No VITE_API_URL env var. -->

<!-- Locked from UI-SPEC.md: -->
<!-- Layout: <aside w-60 border-r border-line p-4 flex flex-col> + <main flex-1 p-6 overflow-auto> -->
<!-- Nav items: lucide icons Package + ArrowLeftRight, classes documented in §"Sidebar Nav Item" -->
<!-- Health pill states (Verificando API… / API conectada / API offline) — icons Loader2 / Check / XCircle -->
<!-- Single H1 on page = wordmark; page titles inside route placeholders are H2 (text-lg font-semibold) -->
<!-- Route placeholders: "Em breve — o cadastro de produtos chega na próxima fase." etc. -->
<!-- Color tokens: brand-50..900 + ink #212121, muted #7B7B7B, line #EBEBEB, surface #F4F4F4, success #009C34, danger #CF2E2E, warning #FCB900 -->

<!-- API contract from Plan 02 (HealthResponse): -->
{
  "status": "ok" | "degraded",
  "db": "up" | "down",
  "version": string,
  "timestamp": string  // ISO 8601 UTC
}
<!-- Health pill maps per UI-SPEC §"Health-Status Pill" (locked): -->
<!-- Connected ← HTTP 2xx AND body.status === "ok" AND body.db === "up" -->
<!-- Offline   ← any other outcome (non-2xx, network error, timeout > 5s, OR 2xx with status != "ok" OR db != "up") -->

<!-- ApiError shape per frontend/CLAUDE.md (we DON'T need to fully implement the interceptor in P1, but the type lives in shared/api/client.ts so Phase 2 picks it up): -->
export interface ApiError {
  errorCode: string
  category: 'VALIDATION' | 'BUSINESS_RULE' | 'NOT_FOUND' | 'INTERNAL'
  message: string
  hint?: string
  statusCode: number
  retryable: boolean
  details?: Record<string, unknown>
  traceId: string
  timestamp: string
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap frontend project (package.json, tsconfig, vite.config, tailwind.config, postcss, eslint/prettier, index.html, .gitignore, env.d.ts)</name>
  <read_first>
    - /home/thallysrc/Projects/DesafioTecnico/frontend/CLAUDE.md §"Stack", §"Tailwind Config", §"Project Structure (Feature-Based)"
    - /home/thallysrc/Projects/DesafioTecnico/.planning/phases/01-foundation/01-CONTEXT.md (D-06, D-07, D-08)
    - /home/thallysrc/Projects/DesafioTecnico/.planning/phases/01-foundation/01-UI-SPEC.md §"Asset Contract", §"Typography"
  </read_first>
  <files>
    - frontend/package.json
    - frontend/package-lock.json
    - frontend/tsconfig.json
    - frontend/tsconfig.node.json
    - frontend/vite.config.ts
    - frontend/tailwind.config.js
    - frontend/postcss.config.js
    - frontend/index.html
    - frontend/.gitignore
    - frontend/.eslintrc.cjs
    - frontend/.prettierrc
    - frontend/env.d.ts
    - frontend/public/favicon.svg
  </files>
  <action>
Initialize the frontend project from scratch (the directory currently only contains `CLAUDE.md` — adding it as Plan 01's `Dockerfile` and `.dockerignore` is fine, those don't conflict).

**Step 1.1 — Write `frontend/package.json`** (use this exact content; pin versions for reproducibility):

```json
{
  "name": "stockeasy-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "type-check": "vue-tsc --noEmit",
    "lint": "eslint . --ext .vue,.ts,.tsx --max-warnings 0",
    "format": "prettier --write \"src/**/*.{vue,ts,tsx,css,json}\""
  },
  "dependencies": {
    "axios": "^1.7.7",
    "lucide-vue-next": "^0.460.0",
    "vue": "^3.5.13",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@typescript-eslint/eslint-plugin": "^8.13.0",
    "@typescript-eslint/parser": "^8.13.0",
    "@vitejs/plugin-vue": "^5.1.4",
    "@vue/eslint-config-typescript": "^14.1.3",
    "autoprefixer": "^10.4.20",
    "eslint": "^9.14.0",
    "eslint-plugin-vue": "^9.30.0",
    "postcss": "^8.4.49",
    "prettier": "^3.3.3",
    "tailwindcss": "^3.4.14",
    "typescript": "~5.6.3",
    "vite": "^5.4.10",
    "vue-tsc": "^2.1.10"
  }
}
```

Notes:
- `lucide-vue-next` per UI-SPEC.md (icons: `Package`, `ArrowLeftRight`, `Loader2`, `Check`, `XCircle`).
- `axios` per FRONT-05/CLAUDE.md.
- No Pinia, no Vee-Validate, no Zod (Phase 2).
- No Vitest yet (Phase 4).
- `build` script runs `vue-tsc --noEmit` first to catch TS errors that `vite build` itself doesn't (Vite skips type-checking by default).

**Step 1.2 — Install dependencies** (also generates `package-lock.json`):

```bash
cd /home/thallysrc/Projects/DesafioTecnico/frontend
npm install
```

**Step 1.3 — Write `frontend/tsconfig.json`** (strict mode — FRONT-01):

```json
{
  "compilerOptions": {
    "target": "ESNext",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "jsx": "preserve",
    "sourceMap": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "allowImportingTsExtensions": false,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },
    "types": ["vite/client"]
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue", "env.d.ts"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

**Step 1.4 — Write `frontend/tsconfig.node.json`** (for vite.config.ts):

```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "types": ["node"]
  },
  "include": ["vite.config.ts"]
}
```

**Step 1.5 — Write `frontend/env.d.ts`** (Vue + Vite client types):

```ts
/// <reference types="vite/client" />

declare module '*.vue' {
  import type { DefineComponent } from 'vue'
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const component: DefineComponent<{}, {}, any>
  export default component
}
```

**Step 1.6 — Write `frontend/vite.config.ts`** (D-06, D-07 — literal proxy target, NO env-var indirection):

```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'node:path'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    host: '0.0.0.0',
    port: 5173,
    strictPort: true,
    proxy: {
      // D-06 / D-07 (LOCKED): forward /api/** to the backend service over Docker DNS.
      // Target is the literal compose service name "backend". No host.docker.internal,
      // no env-var indirection, no fallback. The canonical run path is `docker compose up`.
      '/api': {
        target: 'http://backend:8080',
        changeOrigin: true,
        secure: false,
      },
    },
  },
})
```

Rationale: D-07 is non-negotiable — the Vite proxy target is the literal string `'http://backend:8080'`. No `process.env.BACKEND_HOST`, no ternary, no fallback. The canonical (and only supported) run path is `docker compose up`, where Docker DNS resolves `backend` to the compose service. Running `npm run dev` directly on the host is NOT a supported workflow in Phase 1; the evaluator's path is compose.

**Step 1.7 — Write `frontend/tailwind.config.js`** (FRONT-02 — full palette + semantic tokens + Inter):

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{vue,ts,tsx}'],
  theme: {
    extend: {
      colors: {
        brand: {
          50:  '#EFF4FC',
          100: '#D6E2F7',
          200: '#A6C0EE',
          300: '#759DE5',
          400: '#447BDC',
          500: '#1863DC',
          600: '#0056A7',
          700: '#003F7D',
          800: '#002A54',
          900: '#001530',
        },
        ink:     '#212121',
        muted:   '#7B7B7B',
        line:    '#EBEBEB',
        surface: '#F4F4F4',
        success: '#009C34',
        warning: '#FCB900',
        danger:  '#CF2E2E',
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
    },
  },
  plugins: [],
}
```

**Step 1.8 — Write `frontend/postcss.config.js`**:

```js
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

**Step 1.9 — Write `frontend/index.html`** (FRONT-03 Inter font, FRONT-11 wordmark in title, UI-SPEC asset contract):

```html
<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="StockEasy — gestão de produtos e movimentações de estoque" />
    <title>StockEasy</title>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="font-sans antialiased bg-white text-ink">
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

**Step 1.10 — Write `frontend/.gitignore`**:

```
node_modules
dist
dist-ssr
.vite
*.local
.DS_Store
*.log
.env.local
.env.*.local
```

**Step 1.11 — Write `frontend/.eslintrc.cjs`**:

```js
/* eslint-env node */
module.exports = {
  root: true,
  env: {
    browser: true,
    es2022: true,
    node: true,
  },
  extends: [
    'eslint:recommended',
    'plugin:vue/vue3-recommended',
    'plugin:@typescript-eslint/recommended',
    '@vue/eslint-config-typescript',
  ],
  parser: 'vue-eslint-parser',
  parserOptions: {
    parser: '@typescript-eslint/parser',
    ecmaVersion: 'latest',
    sourceType: 'module',
  },
  rules: {
    'vue/multi-word-component-names': 'off', // we use App.vue, HealthPill.vue etc.
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
    'no-console': ['warn', { allow: ['warn', 'error'] }],
  },
  ignorePatterns: ['dist', 'node_modules', '*.config.{js,ts}'],
}
```

**Step 1.12 — Write `frontend/.prettierrc`**:

```json
{
  "singleQuote": true,
  "semi": false,
  "printWidth": 100,
  "trailingComma": "all",
  "vueIndentScriptAndStyle": true
}
```

**Step 1.13 — Write `frontend/public/favicon.svg`** (UI-SPEC asset contract: stylized "SE" in brand-500 on white square, recognizable at 16px):

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <rect width="32" height="32" rx="6" fill="#1863DC" />
  <text x="16" y="22" font-family="Inter, system-ui, sans-serif" font-size="18" font-weight="700"
        fill="#FFFFFF" text-anchor="middle" letter-spacing="-0.5">SE</text>
</svg>
```

Rules:
- All deps pinned with caret minor (`^`) so `npm install` resolves to current stable patch versions but locked by `package-lock.json` after first install.
- `tsconfig.json` strict mode + `noUnusedLocals` + `noUnusedParameters` + `verbatimModuleSyntax` (required for `import type` discipline in modern Vue 3 + Vite).
- Vite proxy target is the **literal** `http://backend:8080` per D-07 — no env-var indirection, no fallback. The only supported run path is `docker compose up`.
- ESLint v9 still supports `.eslintrc.cjs` via the legacy config loader; we use `.cjs` because the rest of the project is ESM (`"type": "module"`).

Do NOT add: Pinia, Vee-Validate, Zod, Vitest, @vue/test-utils, shadcn-vue, Headless UI, PrimeVue, dayjs, lodash. Those are Phase 2/Phase 4.

**Step 1.14 — Verify scaffold:**

```bash
cd /home/thallysrc/Projects/DesafioTecnico/frontend
npx vue-tsc --noEmit 2>&1 | head -20 || true   # may error since src/ doesn't exist yet — OK
test -f node_modules/vue/package.json
test -f node_modules/vite/package.json
```

`vue-tsc --noEmit` will fail at this stage because `src/main.ts` doesn't exist yet — that's expected. We confirm npm install succeeded by checking node_modules presence; the real `npm run build` happens in Task 3.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/DesafioTecnico/frontend && test -f package.json && test -f package-lock.json && test -f tsconfig.json && test -f vite.config.ts && test -f tailwind.config.js && test -f postcss.config.js && test -f index.html && test -f .gitignore && test -f .eslintrc.cjs && test -f .prettierrc && test -f env.d.ts && test -f public/favicon.svg && test -d node_modules/vue && test -d node_modules/vite && test -d node_modules/axios && test -d node_modules/lucide-vue-next && test -d node_modules/vue-router && test -d node_modules/tailwindcss && grep -q "\"strict\": true" tsconfig.json && grep -q "@/\\*" tsconfig.json && grep -q "target: 'http://backend:8080'" vite.config.ts && ! grep -q "process.env.BACKEND_HOST" vite.config.ts && grep -q "'/api'" vite.config.ts && grep -q "1863DC" tailwind.config.js && grep -q "Inter" tailwind.config.js && grep -q "fonts.googleapis.com/css2?family=Inter" index.html && grep -q "<title>StockEasy</title>" index.html && grep -q "#1863DC" public/favicon.svg</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` exists with scripts: `dev`, `build`, `preview`, `type-check`, `lint`, `format`
    - `frontend/package.json` lists deps: `vue`, `vue-router`, `axios`, `lucide-vue-next`
    - `frontend/package.json` lists devDeps: `vite`, `@vitejs/plugin-vue`, `typescript`, `vue-tsc`, `tailwindcss`, `postcss`, `autoprefixer`, `eslint`, `prettier`
    - `frontend/package-lock.json` exists (proves `npm install` ran)
    - `frontend/node_modules/{vue,vite,axios,lucide-vue-next,vue-router,tailwindcss}` directories exist
    - `frontend/tsconfig.json` contains `"strict": true` (FRONT-01)
    - `frontend/tsconfig.json` declares path alias `"@/*": ["src/*"]`
    - `frontend/vite.config.ts` contains the literal proxy line `target: 'http://backend:8080'` (D-06, D-07) AND `'/api'` proxy key (D-06)
    - `frontend/vite.config.ts` does **NOT** contain `process.env.BACKEND_HOST` (D-07: no env-var indirection) — assert via `! grep -q 'process.env.BACKEND_HOST' vite.config.ts`
    - `frontend/vite.config.ts` sets `host: '0.0.0.0'` and `port: 5173` (INFRA-08)
    - `frontend/tailwind.config.js` contains literal `1863DC` (FRONT-02 — brand-500 hex; simple grep, matches `'#1863DC'` and any surrounding whitespace variants)
    - `frontend/tailwind.config.js` contains `Inter` in `fontFamily.sans`
    - `frontend/index.html` contains the Google Fonts Inter link with weights `400;500;600;700` (FRONT-03)
    - `frontend/index.html` contains `<title>StockEasy</title>` (UI-SPEC.md Copywriting Contract)
    - `frontend/index.html` declares `lang="pt-BR"` and `<div id="app"></div>`
    - `frontend/public/favicon.svg` exists with `#1863DC` somewhere in the file
    - `frontend/.gitignore` excludes `node_modules`, `dist`, `.vite`
    - No Pinia, no Vee-Validate, no Zod in package.json
  </acceptance_criteria>
  <done>
The build chain is wired: TS strict, Tailwind ready, Vite knows how to proxy to backend (literal `http://backend:8080`, no env-var indirection), Inter loaded, favicon in place. Task 2 authors the actual Vue source tree on top.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Vue source tree — main.ts, App.vue, router, AppShell, HealthPill, two placeholder pages, shared Axios client, base CSS</name>
  <read_first>
    - /home/thallysrc/Projects/DesafioTecnico/frontend/CLAUDE.md §"Routing", §"Components", §"HTTP Client", §"Catálogo de classes recorrentes"
    - /home/thallysrc/Projects/DesafioTecnico/.planning/phases/01-foundation/01-UI-SPEC.md (entire file — every component below derives from a section here)
    - /home/thallysrc/Projects/DesafioTecnico/.planning/phases/01-foundation/01-CONTEXT.md (D-08 Axios baseURL '/api', health endpoint contract)
  </read_first>
  <files>
    - frontend/src/main.ts
    - frontend/src/App.vue
    - frontend/src/style.css
    - frontend/src/router/index.ts
    - frontend/src/shared/api/client.ts
    - frontend/src/shared/api/health.ts
    - frontend/src/shared/components/AppShell.vue
    - frontend/src/shared/components/HealthPill.vue
    - frontend/src/features/products/pages/ProductsPage.vue
    - frontend/src/features/stock/pages/StockMovementsPage.vue
    - frontend/src/features/products/.gitkeep
    - frontend/src/features/stock/.gitkeep
  </files>
  <action>
Create each file with exactly the content shown. File-naming follows frontend/CLAUDE.md (PascalCase for .vue, camelCase for .ts).

First create the directories:

```bash
cd /home/thallysrc/Projects/DesafioTecnico/frontend
mkdir -p src/router src/shared/api src/shared/components src/shared/composables src/shared/types \
         src/features/products/components src/features/products/composables src/features/products/pages \
         src/features/stock/components src/features/stock/composables src/features/stock/pages
echo "# placeholder for Phase 2 components/composables/types/schemas/api/pages" > src/features/products/.gitkeep
echo "# placeholder for Phase 2/3 components/composables/types/schemas/api/pages" > src/features/stock/.gitkeep
```

**File 1: `frontend/src/style.css`** (Tailwind + base reset, focus-visible default, Inter applied via tailwind config):

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  body {
    @apply font-sans text-ink bg-white;
  }

  /* Keep a visible focus ring on all interactive elements (UI-SPEC §"Accessibility Floor"). */
  *:focus-visible {
    @apply outline-none ring-2 ring-brand-500 ring-offset-1;
  }
}
```

**File 2: `frontend/src/main.ts`** (bootstrap):

```ts
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import './style.css'

createApp(App).use(router).mount('#app')
```

**File 3: `frontend/src/router/index.ts`** (FRONT-09, FRONT-10):

```ts
import { createRouter, createWebHistory, type RouteRecordRaw } from 'vue-router'

const routes: RouteRecordRaw[] = [
  // FRONT-10: redirect root to /products
  { path: '/', redirect: '/products' },
  {
    path: '/products',
    name: 'products',
    component: () => import('@/features/products/pages/ProductsPage.vue'),
    meta: { title: 'Produtos' },
  },
  {
    path: '/stock-movements',
    name: 'stock-movements',
    component: () => import('@/features/stock/pages/StockMovementsPage.vue'),
    meta: { title: 'Movimentação de Estoque' },
  },
  // Fallback: anything unknown also goes to /products in Phase 1 (no 404 page yet)
  { path: '/:pathMatch(.*)*', redirect: '/products' },
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

// Update document.title from route.meta.title (UI-SPEC §"Accessibility Floor")
router.afterEach((to) => {
  const title = (to.meta?.title as string | undefined) ?? 'StockEasy'
  document.title = title === 'StockEasy' ? 'StockEasy' : `${title} — StockEasy`
})

export default router
```

**File 4: `frontend/src/shared/api/client.ts`** (D-08 — single Axios instance, baseURL '/api', ApiError type pre-declared for Phase 2):

```ts
import axios, { type AxiosError } from 'axios'

/**
 * Canonical error envelope returned by the backend. Phase 2 expands the interceptor below
 * to normalize every rejected request into this shape so UI components consume `.hint` directly.
 */
export interface ApiError {
  errorCode: string
  category: 'VALIDATION' | 'BUSINESS_RULE' | 'NOT_FOUND' | 'INTERNAL'
  message: string
  hint?: string
  statusCode: number
  retryable: boolean
  details?: Record<string, unknown>
  traceId: string
  timestamp: string
}

/**
 * Single Axios instance for the entire app. D-08: baseURL = '/api'. Calls written as
 * `apiClient.get('/health')` resolve to `/api/health`, which the Vite dev server proxies
 * to `http://backend:8080/api/health`.
 */
export const apiClient = axios.create({
  baseURL: '/api',
  timeout: 10_000,
  headers: { 'Content-Type': 'application/json' },
})

// Phase 1 interceptor: lightweight. Phase 2 expands this with full ApiError normalization,
// idempotency-key generation for POST /stock-movements, etc.
apiClient.interceptors.response.use(
  (response) => response,
  (error: AxiosError<ApiError>) => {
    if (error.response?.data?.errorCode) {
      return Promise.reject(error.response.data)
    }
    const fallback: ApiError = {
      errorCode: 'NETWORK_ERROR',
      category: 'INTERNAL',
      message: 'Erro ao se comunicar com o servidor',
      hint: 'Verifique sua conexão e tente novamente.',
      statusCode: error.response?.status ?? 0,
      retryable: true,
      traceId: 'client',
      timestamp: new Date().toISOString(),
    }
    return Promise.reject(fallback)
  },
)

/** UUID v4 generator for Idempotency-Key headers (Phase 3 wires this into movement POSTs). */
export function generateIdempotencyKey(): string {
  return crypto.randomUUID()
}
```

**File 5: `frontend/src/shared/api/health.ts`** (typed wrapper for the only Phase 1 endpoint):

```ts
import { apiClient } from './client'

export interface HealthResponse {
  status: 'ok' | 'degraded'
  db: 'up' | 'down'
  version: string
  timestamp: string
}

/**
 * GET /api/health — pings backend + DB. Called by `HealthPill.vue` on mount.
 * The 5-second timeout matches UI-SPEC §"Health-Status Pill" behavior rule.
 */
export function fetchHealth(signal?: AbortSignal): Promise<HealthResponse> {
  return apiClient
    .get<HealthResponse>('/health', { signal, timeout: 5_000 })
    .then((r) => r.data)
}
```

**File 6: `frontend/src/shared/components/HealthPill.vue`** (UI-SPEC §"Health-Status Pill" — three states):

```vue
<script setup lang="ts">
import { onBeforeUnmount, onMounted, ref } from 'vue'
import { Check, Loader2, XCircle } from 'lucide-vue-next'
import { fetchHealth, type HealthResponse } from '@/shared/api/health'

type PillState = 'checking' | 'connected' | 'offline'

const state = ref<PillState>('checking')
const controller = new AbortController()

onMounted(async () => {
  try {
    const health: HealthResponse = await fetchHealth(controller.signal)
    // UI-SPEC §"Health-Status Pill" (locked): Connected ← 2xx AND status==="ok" AND db==="up".
    // Anything else (2xx with degraded/db-down, or any error) → offline.
    state.value = health.status === 'ok' && health.db === 'up' ? 'connected' : 'offline'
  } catch {
    // Any rejection (network, timeout, 5xx) → offline.
    state.value = 'offline'
  }
})

onBeforeUnmount(() => {
  controller.abort()
})
</script>

<template>
  <div
    v-if="state === 'checking'"
    role="status"
    aria-live="polite"
    class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-surface"
  >
    <Loader2 class="w-4 h-4 text-muted animate-spin" aria-hidden="true" />
    <span class="text-xs text-muted">Verificando API…</span>
  </div>

  <div
    v-else-if="state === 'connected'"
    role="status"
    aria-live="polite"
    class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-success/10"
  >
    <Check class="w-4 h-4 text-success" aria-hidden="true" />
    <span class="text-xs font-medium text-success">API conectada</span>
  </div>

  <div
    v-else
    role="status"
    aria-live="polite"
    class="inline-flex items-center gap-2 px-3 py-1.5 rounded-md bg-danger/10"
  >
    <XCircle class="w-4 h-4 text-danger" aria-hidden="true" />
    <span class="text-xs font-medium text-danger">API offline</span>
  </div>
</template>
```

**File 7: `frontend/src/shared/components/AppShell.vue`** (UI-SPEC §"Layout" + §"Sidebar Nav Item" + FRONT-11 wordmark):

```vue
<script setup lang="ts">
import { ArrowLeftRight, Package } from 'lucide-vue-next'
import { useRoute } from 'vue-router'
import HealthPill from './HealthPill.vue'

interface NavItem {
  basePath: string
  to: string
  label: string
  icon: typeof Package
}

const navItems: NavItem[] = [
  { basePath: '/products', to: '/products', label: 'Produtos', icon: Package },
  {
    basePath: '/stock-movements',
    to: '/stock-movements',
    label: 'Movimentação de Estoque',
    icon: ArrowLeftRight,
  },
]

const route = useRoute()
const isActive = (basePath: string) => route.path.startsWith(basePath)
</script>

<template>
  <div class="flex h-screen bg-white">
    <aside class="w-60 border-r border-line p-4 flex flex-col bg-white">
      <!-- FRONT-11: Wordmark with colorized "Easy". Single H1 on the page (a11y). -->
      <h1 class="text-lg font-semibold text-ink mb-6 px-2">
        Stock<span class="text-brand-500">Easy</span>
      </h1>

      <nav aria-label="Navegação principal" class="flex flex-col gap-1">
        <RouterLink
          v-for="item in navItems"
          :key="item.basePath"
          :to="item.to"
          :class="[
            'flex items-center gap-3 px-4 py-2.5 rounded-md text-sm font-medium transition-colors',
            isActive(item.basePath)
              ? 'bg-brand-50 text-brand-700'
              : 'text-muted hover:bg-surface hover:text-ink',
          ]"
        >
          <component :is="item.icon" class="w-5 h-5" aria-hidden="true" />
          <span>{{ item.label }}</span>
        </RouterLink>
      </nav>

      <!-- Health pill at the bottom of the sidebar (UI-SPEC §"Health-Status Pill"). -->
      <div class="mt-auto pt-4">
        <HealthPill />
      </div>
    </aside>

    <main class="flex-1 p-6 overflow-auto">
      <slot />
    </main>
  </div>
</template>
```

**File 8: `frontend/src/App.vue`** (root layout):

```vue
<script setup lang="ts">
import AppShell from '@/shared/components/AppShell.vue'
</script>

<template>
  <AppShell>
    <RouterView />
  </AppShell>
</template>
```

**File 9: `frontend/src/features/products/pages/ProductsPage.vue`** (UI-SPEC §"Route Placeholder Pattern"):

```vue
<script setup lang="ts">
// Phase 1 placeholder. Real ProductList + ProductForm + DeleteProductModal land in Phase 2.
</script>

<template>
  <section class="max-w-2xl">
    <h2 class="text-lg font-semibold text-ink mb-2">Produtos</h2>
    <p class="text-sm text-muted">Em breve — o cadastro de produtos chega na próxima fase.</p>
  </section>
</template>
```

**File 10: `frontend/src/features/stock/pages/StockMovementsPage.vue`**:

```vue
<script setup lang="ts">
// Phase 1 placeholder. Real Inbound/Outbound forms + MovementHistory land in Phase 3.
</script>

<template>
  <section class="max-w-2xl">
    <h2 class="text-lg font-semibold text-ink mb-2">Movimentação de Estoque</h2>
    <p class="text-sm text-muted">
      Em breve — entradas e saídas de estoque chegam na próxima fase.
    </p>
  </section>
</template>
```

Rules:
- Every `.vue` file uses `<script setup lang="ts">` (frontend/CLAUDE.md §"Single File Component shape").
- Sidebar items use `startsWith(item.basePath)` for active match (UI-SPEC) so future nested routes (e.g. `/products/123`) keep "Produtos" highlighted.
- `HealthPill` aborts its fetch on unmount via `AbortController` — clean teardown.
- `HealthPill` treats any error (including the 5s timeout in `fetchHealth`) as `offline`, never throws into the UI.
- `HealthPill` state-mapping is locked by UI-SPEC §"Health-Status Pill": **Connected ← HTTP 2xx AND body.status === "ok" AND body.db === "up"**; everything else (degraded API, DB down, non-2xx, network error, timeout) → **offline**.
- `App.vue` uses `<AppShell>` as a layout component with `<slot />` so `<RouterView />` is the only thing that swaps per route. The shell never re-mounts.
- `apiClient` baseURL is `/api`, so `fetchHealth` calls `apiClient.get('/health')` which resolves to `/api/health` → Vite proxy → `http://backend:8080/api/health`.
- The 5s timeout in `fetchHealth` overrides the 10s default on `apiClient` for this specific call (UI-SPEC §"Health-Status Pill" "5-second timeout — after that, transition to offline").

Do NOT add: any Phase 2 components (`ProductForm`, `BaseButton`, `BaseModal`, `BaseTable`, `useToast`), any Pinia store, any Vee-Validate forms, the actual product/movement API calls.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/DesafioTecnico/frontend && test -f src/main.ts && test -f src/App.vue && test -f src/style.css && test -f src/router/index.ts && test -f src/shared/api/client.ts && test -f src/shared/api/health.ts && test -f src/shared/components/AppShell.vue && test -f src/shared/components/HealthPill.vue && test -f src/features/products/pages/ProductsPage.vue && test -f src/features/stock/pages/StockMovementsPage.vue && grep -q "createApp(App)" src/main.ts && grep -q "redirect: '/products'" src/router/index.ts && grep -q "/stock-movements" src/router/index.ts && grep -q "baseURL: '/api'" src/shared/api/client.ts && grep -q "crypto.randomUUID" src/shared/api/client.ts && grep -q "apiClient.get<HealthResponse>('/health'" src/shared/api/health.ts && grep -q "Stock<span class=\"text-brand-500\">Easy</span>" src/shared/components/AppShell.vue && grep -q "lucide-vue-next" src/shared/components/AppShell.vue && grep -q "Package" src/shared/components/AppShell.vue && grep -q "ArrowLeftRight" src/shared/components/AppShell.vue && grep -q "bg-brand-50 text-brand-700" src/shared/components/AppShell.vue && grep -q "Verificando API" src/shared/components/HealthPill.vue && grep -q "API conectada" src/shared/components/HealthPill.vue && grep -q "API offline" src/shared/components/HealthPill.vue && grep -q "Loader2" src/shared/components/HealthPill.vue && grep -q "Em breve" src/features/products/pages/ProductsPage.vue && grep -q "Em breve" src/features/stock/pages/StockMovementsPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - All 10 source files exist at the paths listed in <files>
    - `src/main.ts` calls `createApp(App).use(router).mount('#app')` and imports `./style.css`
    - `src/router/index.ts` has `{ path: '/', redirect: '/products' }` (FRONT-10) AND a route for `/stock-movements`
    - `src/router/index.ts` updates `document.title` in `router.afterEach`
    - `src/shared/api/client.ts` exports `apiClient` with literal `baseURL: '/api'` (D-08)
    - `src/shared/api/client.ts` exports `ApiError` interface AND `generateIdempotencyKey` function
    - `src/shared/api/health.ts` exports `fetchHealth` calling `apiClient.get<HealthResponse>('/health', ...)` with 5_000 ms timeout
    - `src/shared/components/AppShell.vue` contains the literal wordmark `Stock<span class="text-brand-500">Easy</span>` (FRONT-11)
    - `src/shared/components/AppShell.vue` imports `Package` and `ArrowLeftRight` from `lucide-vue-next`
    - `src/shared/components/AppShell.vue` active-item class string contains `bg-brand-50 text-brand-700` (UI-SPEC)
    - `src/shared/components/AppShell.vue` has `<aside>` with `w-60` AND `<main>` with `flex-1 p-6 overflow-auto` AND `<nav aria-label="Navegação principal">`
    - `src/shared/components/HealthPill.vue` renders three states with strings "Verificando API…", "API conectada", "API offline"
    - `src/shared/components/HealthPill.vue` imports `Loader2`, `Check`, `XCircle` from `lucide-vue-next`
    - `src/shared/components/HealthPill.vue` uses `AbortController` and aborts on `onBeforeUnmount`
    - `src/shared/components/HealthPill.vue` state mapping requires `status === 'ok' && db === 'up'` for `connected`, all else → `offline` (matches UI-SPEC lock)
    - `src/features/products/pages/ProductsPage.vue` shows "Em breve — o cadastro de produtos chega na próxima fase."
    - `src/features/stock/pages/StockMovementsPage.vue` shows "Em breve — entradas e saídas de estoque chegam na próxima fase."
    - Both placeholder pages use `<h2 class="text-lg font-semibold text-ink mb-2">` (UI-SPEC: single H1 = wordmark)
    - No file references Pinia, Vee-Validate, Zod, or any Phase 2/3-only components
  </acceptance_criteria>
  <done>
The full Vue source tree exists. Task 3 verifies the build pipeline, dev server, and lint all succeed end-to-end on the host (Docker run is validated in Plan 04).
  </done>
</task>

<task type="auto" smoke="true">
  <name>Task 3: Verify type-check, build, lint, and dev server smoke (host run)</name>
  <read_first>
    - /home/thallysrc/Projects/DesafioTecnico/frontend/package.json (scripts created in Task 1)
    - /home/thallysrc/Projects/DesafioTecnico/frontend/src/router/index.ts (routes to smoke)
    - /home/thallysrc/Projects/DesafioTecnico/frontend/vite.config.ts (proxy target is literal `http://backend:8080`; outside compose the proxy won't resolve, but the SPA index still serves)
  </read_first>
  <files>
    - (none — verification-only task)
  </files>
  <behavior>
    - `npm run type-check` (vue-tsc --noEmit) exits 0 with no TypeScript errors under strict mode
    - `npm run build` produces a `dist/` directory with `index.html` and a hashed `assets/` folder
    - `npm run lint` exits 0 (zero ESLint errors AND zero warnings, since `--max-warnings 0`)
    - `npm run dev` (in background) serves on http://localhost:5173 and the root URL returns HTML that includes the string `id="app"` and the Inter font preconnect link
    - The dev server's `/products` and `/stock-movements` HTML routes are served by the SPA index (Vite serves index.html for all unknown paths under history fallback)
    - With no backend running AND no Docker DNS, hitting `/api/health` through the dev server will fail to resolve `backend` — that's expected; the SPA shell load itself does NOT depend on the proxy and is the only thing this host-smoke validates. The full proxy + CORS smoke happens INSIDE docker-compose in Plan 04.
  </behavior>
  <action>
Run these commands in order from `/home/thallysrc/Projects/DesafioTecnico/frontend/`. Each must succeed before the next. Stop and surface the first failure.

```bash
cd /home/thallysrc/Projects/DesafioTecnico/frontend

# 1. Type-check (FRONT-01 strict mode)
npm run type-check

# 2. Production build (catches accidental imports of missing files, dead types, etc.)
npm run build

# 3. Lint (zero warnings tolerated by `--max-warnings 0` in the script)
npm run lint

# 4. Dev server smoke — start vite, hit it once, kill it.
# The Vite proxy target is the literal `http://backend:8080` (D-07). Outside docker-compose,
# the proxy itself cannot resolve `backend`, but the SPA index loads fine without ever
# talking to the backend. We only assert the SPA shell here. The full proxy + CORS smoke
# runs INSIDE docker-compose in Plan 04.
npm run dev > /tmp/vite.log 2>&1 &
VITE_PID=$!
for i in $(seq 1 30); do
  if curl -fsS http://localhost:5173/ > /tmp/spa.html 2>/dev/null; then break; fi
  sleep 1
done
# Fetch the root + both routes (Vite dev server serves the same index.html for SPA routes)
curl -fsS http://localhost:5173/ > /tmp/root.html
curl -fsS http://localhost:5173/products > /tmp/products.html
curl -fsS http://localhost:5173/stock-movements > /tmp/movements.html

# Kill the dev server
kill $VITE_PID 2>/dev/null || true
wait $VITE_PID 2>/dev/null || true

# Assert: all three HTML responses contain the SPA root + the Inter preconnect
for f in /tmp/root.html /tmp/products.html /tmp/movements.html; do
  test -s "$f"
  grep -q 'id="app"' "$f"
  grep -q 'fonts.googleapis.com/css2?family=Inter' "$f"
  grep -q '<title>StockEasy</title>' "$f"
done

# Confirm `dist/` was produced by the build
test -f /home/thallysrc/Projects/DesafioTecnico/frontend/dist/index.html
ls /home/thallysrc/Projects/DesafioTecnico/frontend/dist/assets/*.js > /dev/null
```

If any step fails:
- `type-check` errors → fix the TS issue in the offending file from Task 2 (most common: missing `import type`, missing `as const` on a literal, unused parameter).
- `build` errors that didn't show up in type-check → missing module import, missing `.vue` file referenced by the router (lazy import errors surface here).
- `lint` errors → run `npm run format` to auto-fix Prettier formatting; for ESLint-only failures fix manually.
- `curl` timeout → check `/tmp/vite.log` — most likely a port collision (something else on 5173). The fix is to kill the offender; do NOT change the port (must stay 5173 per INFRA-08).

Cleanup after the task:

```bash
rm -rf /home/thallysrc/Projects/DesafioTecnico/frontend/dist
rm -f /tmp/vite.log /tmp/spa.html /tmp/root.html /tmp/products.html /tmp/movements.html
```

The cleanup keeps `dist/` out of git (it's already in `.gitignore`) but also prevents stale build artifacts from confusing later tasks.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/DesafioTecnico/frontend && npm run type-check && npm run build && npm run lint && rm -rf dist</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits with code 0 (no TypeScript errors)
    - `npm run build` exits with code 0 and produces `dist/index.html` + `dist/assets/*.js`
    - `npm run lint` exits with code 0 (zero ESLint warnings or errors — `--max-warnings 0` is in the script)
    - Dev server smoke: `curl http://localhost:5173/` returns HTML containing `id="app"`, `<title>StockEasy</title>`, and the Inter font preconnect link
    - Dev server smoke: `curl http://localhost:5173/products` and `curl http://localhost:5173/stock-movements` both return the SPA index (Vite SPA history-fallback)
    - No assertion on the proxy target from host (proxy needs docker-compose DNS; Plan 04 runs that smoke inside compose)
    - `dist/` cleaned up after the run so it doesn't end up committed
  </acceptance_criteria>
  <done>
The frontend builds cleanly under TS strict, lints with zero warnings, and the dev server serves the SPA on port 5173. Plan 04 (Wave 2) will run the same kind of smoke INSIDE docker-compose to validate the proxy reaches the live backend and the health pill flips to "API conectada".
  </done>
</task>

</tasks>

<verification>
After all three tasks:

1. **Filesystem layout matches frontend/CLAUDE.md exactly:**
   ```
   frontend/
   ├── CLAUDE.md
   ├── Dockerfile           (from Plan 01)
   ├── .dockerignore        (from Plan 01)
   ├── package.json
   ├── package-lock.json
   ├── tsconfig.json
   ├── tsconfig.node.json
   ├── vite.config.ts
   ├── tailwind.config.js
   ├── postcss.config.js
   ├── index.html
   ├── env.d.ts
   ├── .gitignore
   ├── .eslintrc.cjs
   ├── .prettierrc
   ├── public/favicon.svg
   └── src/
       ├── main.ts
       ├── App.vue
       ├── style.css
       ├── router/index.ts
       ├── shared/
       │   ├── api/{client.ts, health.ts}
       │   └── components/{AppShell.vue, HealthPill.vue}
       └── features/
           ├── products/{pages/ProductsPage.vue, .gitkeep}
           └── stock/{pages/StockMovementsPage.vue, .gitkeep}
   ```
2. **Build + type-check + lint all pass** on the host.
3. **Dev server serves on :5173** and the SPA renders.
</verification>

<success_criteria>
- All 8 requirements (FRONT-01..04, FRONT-09..11, INFRA-08) deliverable from this plan's artifacts
- TypeScript strict mode enabled and passing (FRONT-01)
- Tailwind `brand-*` palette + semantic tokens + Inter wired (FRONT-02, FRONT-03)
- Feature-based folder layout in place (FRONT-04)
- Routes `/products` and `/stock-movements` resolve + `/` redirects to `/products` (FRONT-09, FRONT-10)
- Wordmark `Stock<span class="text-brand-500">Easy</span>` in sidebar (FRONT-11)
- Vite serves on :5173 with hot reload (INFRA-08)
- Vite proxy `/api → http://backend:8080` configured as a literal string per D-07 (no env-var indirection)
- Axios baseURL '/api' (D-08)
- Health pill component fetches `/api/health` on mount with 5s timeout and renders three states (UI-SPEC locked mapping)
- ZERO files touched by Plan 01 (Dockerfile, docker-compose.yml, init.sql) and ZERO files touched by Plan 02 (backend/)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` documenting:
- Pinned dependency versions (Vue 3.5.x, Vite 5.4.x, Tailwind 3.4.x, axios 1.7.x, lucide-vue-next 0.460.x)
- Vite proxy target is the **literal** `http://backend:8080` per D-07 — no env-var indirection, no fallback. The only supported run path is `docker compose up`. (Removed the `BACKEND_HOST` escape hatch from the prior draft.)
- Health pill mapping locked by UI-SPEC: Connected requires `status === 'ok' && db === 'up'`; degraded API or DB-down shows "API offline"
- Health pill 5s timeout overriding apiClient 10s default
- Feature folders `products/` and `stock/` contain only pages + .gitkeep — composables/components/api/schemas land in Phase 2/3
- Confirm Inter font loaded with weights 400;500;600;700 (UI-SPEC §"Typography")
</output>
</content>
</invoke>