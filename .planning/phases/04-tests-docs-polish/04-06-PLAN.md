---
phase: 04-tests-docs-polish
plan: 06
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
  - "04-05"
files_modified:
  - README.md
autonomous: true
requirements_addressed:
  - TEST-07
  - DOC-09
must_haves:
  truths:
    - "Root README.md mentions all four pillars an evaluator wants to confirm in 30 seconds: how to run (docker compose up), how to read the docs (docs/dist/*.pdf links), how to run tests (dotnet test + npm test), and where the Swagger lives (http://localhost:8080/swagger)"
    - "Running `cd backend && dotnet test` from the host exits 0 with all Phase 4 added tests green (Plan 04-01)"
    - "Running `cd frontend && npm install && npm test` from the host exits 0 with all Plan 04-02 + 04-03 tests green"
    - "Root README.md has explicit links to the three branded PDFs in `docs/dist/` — evaluator clicks the link and reads the document"
    - "The original challenge spec link (README.challenge-spec.md) is preserved per CONTEXT.md D-20"
  artifacts:
    - path: README.md
      provides: "Polished root README — quick-start, URLs table, stack one-liner, docs links, tests one-liner, challenge spec link"
      contains: "docs/dist"
  key_links:
    - from: "README.md"
      to: "docs/dist/01-product-decisions.pdf + 02-architecture.pdf + 03-business-rules.pdf"
      via: "explicit markdown link in a 'Documentação' section"
      pattern: "docs/dist/.*\\.pdf"
    - from: "README.md tests section"
      to: "backend/Inventory.Tests + frontend Vitest"
      via: "documented one-liners for both runners"
      pattern: "dotnet test"
---

<objective>
Final phase 4 integration. Polish the root README.md (DOC-09) to include the four evaluator-facing pillars: docker-compose run, Swagger URL, links to `docs/dist/*.pdf`, and the test one-liners. Then run both test suites end-to-end (`dotnet test` + `npm test`) to satisfy TEST-07 ("CI local" green). Per CONTEXT.md D-20: **extend** the existing root README, do NOT replace — Phase 1 + Phase 3 already shipped it with the docker-compose section + the Phase 3 capabilities block.

Purpose: Satisfy TEST-07 (both `dotnet test` and `npm test` exit 0 from a clean local checkout) + DOC-09 (root README polish).

Output: 1 modified README.md + 2 successful test runs (evidence captured in the SUMMARY).

This plan depends on Plans 04-01, 04-02, 04-03 (the tests must exist before TEST-07 verification), and Plan 04-05 (PDFs must exist before README.md links to them). It does NOT depend on Plan 04-04 directly because 04-05 transitively depends on 04-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-tests-docs-polish/04-CONTEXT.md
@README.md
@README.challenge-spec.md
@docker-compose.yml
@backend/Inventory.Tests/Inventory.Tests.csproj
@frontend/package.json

# Outputs that must exist for verification
@docs/dist/01-product-decisions.pdf
@docs/dist/02-architecture.pdf
@docs/dist/03-business-rules.pdf
@backend/Inventory.Tests/Services/ProductServiceTests.cs
@backend/Inventory.Tests/Services/StockMovementServiceTests.cs
@backend/Inventory.Tests/Validators/CreateMovementRequestValidatorTests.cs
@frontend/src/features/products/composables/useProducts.test.ts
@frontend/src/features/stock/composables/useStockMovements.test.ts
@frontend/src/features/products/components/ProductForm.test.ts
@frontend/src/features/stock/components/OutboundForm.test.ts
@frontend/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish root README.md — append/refine the four evaluator-pillars (docker, docs, tests, challenge spec link)</name>
  <files>
    - README.md (modified — extend, do NOT replace)
  </files>
  <read_first>
    - README.md (current state — Phase 1 + Phase 3 already extended it; identify which sections exist and which need adding)
    - README.challenge-spec.md (confirm the link target still exists; preserve the reference)
    - .planning/phases/04-tests-docs-polish/04-CONTEXT.md §D-20 (extension rules: keep what's there; append/refine 5 pillars)
    - docker-compose.yml (URL/port reference)
    - docs/README.md (Plan 04-04 — to mirror the same phrasing in the root README's docs section)
  </read_first>
  <behavior>
    After the edit, README.md MUST contain (sections can be in any sensible order, but ALL must be present):

    1. **Wordmark + tagline** (already present from Phase 1) — preserve
    2. **Stack table** (already present) — preserve, optionally add a "Tests" row if missing
    3. **"Como rodar" section with `docker compose up` + 30-60 s wait** (already present) — preserve
    4. **URLs table** with all four URLs:
       - Frontend (SPA): `http://localhost:5173`
       - Swagger UI: `http://localhost:8080/swagger`
       - Health check: `http://localhost:8080/api/health`
       - Postgres (psql): `psql -h localhost -p 5432 -U stockeasy -d stockeasy` (password `stockeasy123`)
       (Already present from Phase 1 — preserve.)
    5. **"Status"/"Capacidades" section** (already present from Phase 3) — preserve, add a row or note that Phase 4 (tests + docs) is now complete
    6. **NEW: "Documentação" section** linking to each PDF. Each link is a markdown link to `docs/dist/{slug}.pdf` with a 1-line abstract. Example:
       ```markdown
       ## Documentação

       Documentação detalhada com branding StockEasy (paleta `#1863DC`, Inter, capa + cabeçalho + numeração de páginas):

       - [`docs/dist/01-product-decisions.pdf`](docs/dist/01-product-decisions.pdf) — visão do produto, decisões estratégicas (.NET 8 + Dapper + PG16, API agentic, soft delete, idempotency), trade-offs e roadmap v2 (chat LLM)
       - [`docs/dist/02-architecture.pdf`](docs/dist/02-architecture.pdf) — diagramas Mermaid (system context, layered, sequence stock-out, ER, frontend feature flow), tabela de stack e instruções de execução
       - [`docs/dist/03-business-rules.pdf`](docs/dist/03-business-rules.pdf) — entidades, enums, regras enforced (soft delete, idempotência, FOR UPDATE, zero N+1, imutabilidade) e catálogo completo de `errorCodes` (9 códigos)

       Os PDFs já estão commitados — o avaliador abre direto, sem instalar nada. Para regerar localmente, ver `docs/README.md` (Pandoc + WeasyPrint + mermaid-filter).
       ```

    7. **NEW: "Testes" section** with one-liner commands for each runner. Example:
       ```markdown
       ## Testes

       **Backend (xUnit + Moq):**

       ```bash
       cd backend && dotnet test
       ```

       Cobre `ProductService`, `StockMovementService` (incluindo idempotency replay), `CreateProductRequestValidator` e `CreateMovementRequestValidator`. Cada cenário de exception assert a tríade (`ErrorCode`, substring de `Message`, `Hint` não-nulo) via helper `AssertDomain.Trio`.

       **Frontend (Vitest + @vue/test-utils + happy-dom):**

       ```bash
       cd frontend && npm install && npm test
       ```

       Cobre `useProducts`, `useStockMovements` (composables com mock da camada api), `ProductForm` (montagem + emit) e `OutboundForm` (UX-08 helper de saldo + abertura do modal CONF-01 + chamada do `register`).

       Per `TEST-07`: ambos suites rodam em CI local — sem GitHub Actions (escopo explicitamente fora per CONTEXT.md D-24).
       ```

    8. **Challenge spec link** — preserve the existing `> Spec original em [README.challenge-spec.md]` line if present; if not, add it near the top under the wordmark/tagline.

    Do NOT remove or rewrite the existing Phase 1 + Phase 3 sections. The diff for this task should be ADDITIVE — only new sections plus minor refinement of existing ones (e.g., updating the Status table to mark Phase 4 entregue).
  </behavior>
  <action>
    1. Read `README.md` fully first. Identify which sections exist and decide where to slot the new Documentação + Testes sections (suggest: after Status, before any closing notes).

    2. Use Edit (not Write) to append the new sections. If the Status/Capacidades table exists, add a row for Phase 4:
       ```
       | 4. Tests, Docs & Polish | xUnit + Vitest verdes + 3 PDFs branded em docs/dist/ | entregue |
       ```

    3. After edits, re-read the file and confirm:
       - `grep -c "^## " README.md` — count of H2 sections should INCREASE by 2 (Documentação + Testes) vs the pre-edit count
       - `grep -q "docs/dist/01-product-decisions.pdf" README.md`
       - `grep -q "docs/dist/02-architecture.pdf" README.md`
       - `grep -q "docs/dist/03-business-rules.pdf" README.md`
       - `grep -q "dotnet test" README.md`
       - `grep -q "npm test" README.md`
       - `grep -q "README.challenge-spec.md" README.md` (the original spec link is preserved)

    4. Do NOT touch any backend/frontend/docs file in this task.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "docs/dist/01-product-decisions.pdf" README.md && grep -q "docs/dist/02-architecture.pdf" README.md && grep -q "docs/dist/03-business-rules.pdf" README.md && grep -q "dotnet test" README.md && grep -q "npm test" README.md && grep -q "README.challenge-spec.md" README.md && grep -q "http://localhost:8080/swagger" README.md && grep -q "http://localhost:5173" README.md && grep -q "docker compose" README.md && [ "$(grep -cE '^##[[:space:]]+(Documentação|Documentacao)' README.md)" -ge 1 ] && [ "$(grep -cE '^##[[:space:]]+Testes' README.md)" -ge 1 ]</automated>
  </verify>
  <done>
    - README.md has new H2 sections "Documentação" and "Testes"
    - All three PDF paths (`docs/dist/01-product-decisions.pdf` etc) are linked
    - Both test one-liners (`dotnet test` and `npm test`) are present
    - Original Phase 1 sections (docker compose up, URLs table, structure tree) preserved
    - Challenge spec link preserved
    - No production code touched
  </done>
</task>

<task type="auto" smoke="true">
  <name>Task 2: TEST-07 final smoke — run both test suites end-to-end + capture evidence</name>
  <files>
    - (no file modifications — evidence is captured in the SUMMARY at plan close)
  </files>
  <read_first>
    - backend/Inventory.Tests/ (confirm all Plan 04-01 test files are in place)
    - frontend/src/features/products/composables/useProducts.test.ts (Plan 04-02)
    - frontend/src/features/stock/composables/useStockMovements.test.ts (Plan 04-02)
    - frontend/src/features/products/components/ProductForm.test.ts (Plan 04-03)
    - frontend/src/features/stock/components/OutboundForm.test.ts (Plan 04-03)
    - frontend/vitest.config.ts (Plan 04-02)
  </read_first>
  <behavior>
    Final integration smoke for TEST-07. The evaluator's mental model: clone the repo, install deps, run two commands, see green.

    1. **Backend smoke:**
       ```bash
       cd /home/thallysrc/Projects/DesafioTecnico/backend && dotnet test --logger "console;verbosity=normal"
       ```
       MUST report `Failed: 0` and a `Passed: N` count where `N` >= total tests added in Plan 04-01 + Phase 2's existing 8 = roughly ≥ 30. Capture the exact output for the SUMMARY.

    2. **Frontend smoke:**
       ```bash
       cd /home/thallysrc/Projects/DesafioTecnico/frontend && npm install && npm test
       ```
       (`npm install` may already have completed during Plan 04-02 — re-running is idempotent and confirms a clean reinstall produces the same green output. If `npm install` fails due to network restrictions, document the error in the SUMMARY and let the orchestrator escalate.)

       MUST report:
       - `Test Files  X passed (X)` where X ≥ 4 (2 composable + 2 component)
       - `Tests  Y passed (Y)` where Y ≥ 27 (≥ 18 from 04-02 + ≥ 9 from 04-03)
       - exit code 0

    3. **No production code modifications:** verify with git status that the only uncommitted changes are .planning/, README.md (from Task 1), or possibly node_modules-related artifacts. Production code in `backend/Inventory/` and `frontend/src/features/{products,stock}/` (excluding `.test.ts` files) MUST be untouched in this plan's diff. Run:
       ```bash
       cd /home/thallysrc/Projects/DesafioTecnico && git diff --name-only HEAD -- backend/Inventory/ frontend/src/features/products/composables/useProducts.ts frontend/src/features/products/components/ProductForm.vue frontend/src/features/stock/composables/useStockMovements.ts frontend/src/features/stock/components/OutboundForm.vue
       ```
       Output MUST be empty.
  </behavior>
  <action>
    1. Run the backend smoke:
       ```bash
       cd /home/thallysrc/Projects/DesafioTecnico/backend && dotnet test --logger "console;verbosity=normal" 2>&1 | tee /tmp/04-06-backend.log
       ```
       Confirm `Passed:` is positive and `Failed:` is 0.

    2. Run the frontend smoke. If `frontend/node_modules` is present and up-to-date, you can skip `npm install` for speed, but the canonical evaluator path is `npm install && npm test`. Run both:
       ```bash
       cd /home/thallysrc/Projects/DesafioTecnico/frontend && npm install 2>&1 | tail -20 | tee /tmp/04-06-npm-install.log
       cd /home/thallysrc/Projects/DesafioTecnico/frontend && npm test 2>&1 | tee /tmp/04-06-frontend.log
       ```
       Confirm `Test Files` line + `Tests` line both show only "passed", no "failed".

    3. Verify no production code was modified by this plan:
       ```bash
       cd /home/thallysrc/Projects/DesafioTecnico && git status --short backend/Inventory/ frontend/src/features/products/composables/useProducts.ts frontend/src/features/products/components/ProductForm.vue frontend/src/features/stock/composables/useStockMovements.ts frontend/src/features/stock/components/OutboundForm.vue
       ```
       Output MUST be empty (no modified/staged production files).

    4. Capture the relevant snippets from `/tmp/04-06-backend.log` and `/tmp/04-06-frontend.log` for the SUMMARY:
       - The "Test Run Successful" / "Passed!" line + total counts (backend)
       - The "Test Files / Tests / Duration" block (frontend Vitest output)

    5. If EITHER suite fails:
       - This is NOT a "fix it in 04-06" situation. The failure means a prior plan (04-01/02/03) has a bug. Stop, file the failure as a Phase 4 gap, and let the orchestrator route to `--gaps` planning per CONTEXT.md D-26 ("If a test reveals an existing bug, scope decision: file as a gap and re-plan, do not silently patch in this phase").
       - Document the failure in this plan's SUMMARY with the exact failing test name + error message + which earlier plan owns it.
  </action>
  <verify>
    <automated>cd backend && dotnet test --logger "console;verbosity=normal" 2>&1 | tee /tmp/04-06-backend-final.log && grep -qE "Failed:[[:space:]]+0" /tmp/04-06-backend-final.log && grep -qE "Passed:[[:space:]]+[1-9][0-9]+" /tmp/04-06-backend-final.log && cd /home/thallysrc/Projects/DesafioTecnico/frontend && npm test 2>&1 | tee /tmp/04-06-frontend-final.log && grep -qE "Test Files[[:space:]]+[4-9][[:space:]]+passed" /tmp/04-06-frontend-final.log && grep -qE "Tests[[:space:]]+[0-9]+[[:space:]]+passed" /tmp/04-06-frontend-final.log && ! grep -qE "Tests[[:space:]]+[1-9][0-9]*[[:space:]]+failed" /tmp/04-06-frontend-final.log && cd /home/thallysrc/Projects/DesafioTecnico && [ -z "$(git status --short backend/Inventory/Services/ backend/Inventory/Controllers/ backend/Inventory/Repositories/ backend/Inventory/Exceptions/ backend/Inventory/Validators/ backend/Inventory/Entities/ backend/Inventory/Middleware/ backend/Inventory/Dtos/ backend/Inventory/Infra/ frontend/src/features/products/composables/useProducts.ts frontend/src/features/products/components/ProductForm.vue frontend/src/features/stock/composables/useStockMovements.ts frontend/src/features/stock/components/OutboundForm.vue)" ]</automated>
  </verify>
  <done>
    - `dotnet test` from backend/ reports `Passed: N` (N ≥ ~30) and `Failed: 0`
    - `npm test` from frontend/ reports `Test Files ≥4 passed` and `Tests ≥27 passed` and zero failed
    - Git status confirms zero modifications to production directories listed above
    - Both log files captured at `/tmp/04-06-*-final.log` for the SUMMARY
  </done>
</task>

</tasks>

<verification>
- Root README.md has Documentação + Testes sections with PDF links + test one-liners
- `dotnet test` exits 0 from a fresh shell
- `npm install && npm test` exits 0 from a fresh shell
- Zero production code modified in Phase 4
- Original Phase 1 + Phase 3 README content preserved (additive edit only)
</verification>

<success_criteria>
- TEST-07 satisfied: both `dotnet test` and `npm test` run sem falhas em CI local
- DOC-09 satisfied: root README links docker-compose run + Swagger URL + docs/dist PDFs + tests
- Phase 4 closes with 5 ROADMAP success criteria met (xUnit green, Vitest green, 3 markdown sources, 3 branded PDFs, polished README)
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests-docs-polish/04-06-SUMMARY.md` documenting:
- Backend `dotnet test` final counts (total / passed / failed = 0)
- Frontend Vitest final block (Test Files / Tests / Duration)
- Diff summary for README.md (sections added)
- Confirmation that no production code was modified in Phase 4 (`git diff` over the production directories is empty across all 6 plans)
- TEST-07 + DOC-09 marked satisfied
- Phase 4 success-criteria checklist (5 items from ROADMAP) marked ✓
</output>
